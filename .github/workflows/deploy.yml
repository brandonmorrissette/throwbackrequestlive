name: Deploy App

on:
    workflow_dispatch:
        inputs:
            deploy-steps:
                description: 'Comma-separated list of steps to run (default: Empty to run all)'
                required: false
                default: ''

jobs:
    deploy-network-stack:
        if: ${{ github.event.inputs.deploy-steps == '' || contains(github.event.inputs.deploy-steps, 'NetworkStack') }}
        uses: ./.github/workflows/deployStack.yml
        with:
            stack-name: NetworkStack
        secrets:
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            AWS_REGION: ${{ secrets.AWS_REGION }}

    deploy-user-stack:
        if: ${{ github.event.inputs.deploy-steps == '' || contains(github.event.inputs.deploy-steps, 'NetworkStack') }}
        uses: ./.github/workflows/deployStack.yml
        with:
            stack-name: NetworkStack
        secrets:
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            AWS_REGION: ${{ secrets.AWS_REGION }}

    deploy-compute-stack:
        if: ${{ github.event.inputs.deploy-steps == '' || contains(github.event.inputs.deploy-steps, 'ComputeStack') }}
        needs: deploy-network-stack
        uses: ./.github/workflows/deployStack.yml
        with:
            stack-name: ComputeStack
        secrets:
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            AWS_REGION: ${{ secrets.AWS_REGION }}

    deploy-storage-stack:
        if: ${{ github.event.inputs.deploy-steps == '' || contains(github.event.inputs.deploy-steps, 'StorageStack') }}
        needs: deploy-network-stack
        uses: ./.github/workflows/deployStack.yml
        with:
            stack-name: StorageStack
        secrets:
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            AWS_REGION: ${{ secrets.AWS_REGION }}

    deploy-runtime-stack:
        if: ${{ github.event.inputs.deploy-steps == '' || contains(github.event.inputs.deploy-steps, 'RuntimeStack') }}
        needs: [deploy-compute-stack, deploy-storage-stack]
        uses: ./.github/workflows/deployReactStack.yml
        with:
            stack-name: RuntimeStack
        secrets:
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            AWS_REGION: ${{ secrets.AWS_REGION }}

    create-superuser:
        if: ${{ github.event.inputs.deploy-steps == '' || contains(github.event.inputs.deploy-steps, 'CreateSuperuser') }}
        needs: deploy-user-stack
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v2
            - name: Invoke Lambda to create Cognito user
              run: |
                  aws lambda invoke \
                  --function-name create-superuser-lambda \
                  --payload '{"email": "${{ vars.SUPERUSER_EMAIL}}"}' \
                  response.json \
                  --cli-binary-format raw-in-base64-out
              env:
                  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                  AWS_REGION: ${{ secrets.AWS_REGION }}

name: Deploy SQL Files

on:
    workflow_call:
        inputs:
            environment:
                required: true
                type: string
        secrets:
            AWS_ACCESS_KEY_ID:
                required: true
            AWS_SECRET_ACCESS_KEY:
                required: true
            AWS_REGION:
                required: true

jobs:
    fetch-cdk-outputs:
        name: Fetch CDK Outputs
        uses: ./.github/workflows/fetch_cdk_outputs.yml
        with:
            environment: ${{ inputs.environment }}
        secrets:
            AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
            AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            AWS_REGION: ${{ secrets.AWS_REGION }}

    run-sql-task:
        needs: fetch-cdk-outputs
        runs-on: ubuntu-latest
        steps:
            - name: Run Fargate Task
              run: |
                  echo "ECS Cluster: ${{ needs.fetch-cdk-outputs.outputs.ecsclustername }}"
                  echo "SQL Task Definition ARN: ${{ needs.fetch-cdk-outputs.outputs.sqltaskdefarn }}"
                  echo "Subnet ID: ${{ needs.fetch-cdk-outputs.outputs.subnetid }}"
                  echo "Security Group ID: ${{ needs.fetch-cdk-outputs.outputs.securitygroupid }}"
                  echo "Hard Code Test: ${{ needs.fetch-cdk-outputs.outputs.hard-code-test }}"

                  aws ecs run-task \
                      --cluster ${{ needs.fetch-cdk-outputs.outputs.ecsclustername }} \
                      --task-definition ${{ needs.fetch-cdk-outputs.outputs.sqltaskdefarn }}  \
                      --launch-type FARGATE \
                      --network-configuration "awsvpcConfiguration={subnets=[${{ needs.fetch-cdk-outputs.outputs.subnetid }}],securityGroups=[${{ needs.fetch-cdk-outputs.outputs.securitygroupid }}],assignPublicIp=ENABLED}"
              env:
                  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                  AWS_REGION: ${{ secrets.AWS_REGION }}
